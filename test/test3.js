function celc_to_far(celsius) {
    // program to convert celsius to fahrenheit

    // calculate fahrenheit
    const fahrenheit = (celsius * 1.8) + 32

    // display the result
    return fahrenheit
}
